frontend = {
    host = "0.0.0.0"
    port = "8443"
    pem-file = {
        cert = "/certs/server.crt"
        private-key = "/certs/server.key"
    }
}

frontend = {
    host = "0.0.0.0"
    port = "8081"
    pem-file = {
        cert = "/certs/server.crt"
        private-key = "/certs/server.key"
    }
}
